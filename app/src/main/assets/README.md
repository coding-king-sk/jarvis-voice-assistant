# Assets folder

Yahan teen ONNX files daalni hain (wake word ke liye):

| File | Kahan se |
| --- | --- |
| `melspectrogram.onnx` | openWakeWord repo (ready-made) |
| `embedding_model.onnx` | openWakeWord repo (ready-made) |
| `wakeword.onnx` | aapka khud ka trained model |

Steps: [`docs/WAKE_WORD.md`](../../../../docs/WAKE_WORD.md)

Bina in files ke bhi app chalega — bas wake word band rahega, mic button se bol sakte ho.
